// Pickups: things the player collects by touching them. Nothing imports this yet — wiring
// coins into the level, the scene, and the HUD is a later ticket.
export type { CoinSpawn } from './coin.ts'
export { COIN_COLOR, COIN_HEIGHT, COIN_WIDTH, Coin, createCoin } from './coin.ts'
